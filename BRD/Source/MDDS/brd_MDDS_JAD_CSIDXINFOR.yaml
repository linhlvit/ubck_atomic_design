schema_type: brd_source_columns
source: MDDS
table: JAD_CSIDXINFOR
bronze_table: JAD_CSIDXINFOR
description: "Thành phần rổ chỉ số: cặp (IndexCode × Symbol) với khối lượng và ngày vào rổ theo ngày giao dịch"
scope_status: in_scope

columns:
  - name: ID
    data_type: "character varying(255)"
    nullable: false
    description: "GUID — PK của bảng"
  - name: INDEXCODE
    data_type: "character varying(255)"
    nullable: true
    description: "Mã chỉ số (FK đến MarketInfor / IDXInfor)"
  - name: SYMBOL
    data_type: "character varying(255)"
    nullable: true
    description: "Mã chứng khoán trong rổ (FK đến StockInfor)"
  - name: TOTALQTTY
    data_type: "character varying(255)"
    nullable: true
    description: "Tổng khối lượng khớp lệnh lô chẵn"
  - name: ADDDATE
    data_type: "character varying(255)"
    nullable: true
    description: "Ngày thêm vào chỉ số"
  - name: INDEXID
    data_type: int
    nullable: true
    description: "ID nội bộ của chỉ số"
  - name: FLOORCODE
    data_type: "character varying(255)"
    nullable: true
    description: "Mã sở giao dịch CK"
  - name: TRADINGDATE
    data_type: "character varying(255)"
    nullable: true
    description: "Ngày giao dịch dạng dd/MM/yyyy"
